- if @short_url.present? && !@short_url.valid?
	%p Invalid URL: #{@short_url.name}

%form(action="/" method="POST")
	%label(for="url")
	%input(type="text" name="url" id="url" value = "http://")
	%input(type="submit" value="shorten")
%p
= flash[:notice]

- if @short_url
	Your shortened url for #{@short_url.name} is
	%a{:href => "#{@short_url.name}"}
		= request.url + @short_url.short
